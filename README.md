# sinotrack-decoder

Firmware (or companion MCU code) that cleanly decodes SinoTrack’s binary GPS packets and converts them to JSON

- Decode GPS position, speed, heading, time

- Output JSON over UART / USB

- Easy integration with servers
